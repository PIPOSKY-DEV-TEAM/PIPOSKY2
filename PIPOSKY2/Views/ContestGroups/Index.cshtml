@{
    ViewBag.Title = "课程";
}

@using PIPOSKY2.Models;
@model IEnumerable<ContestGroup>

<h2>课程</h2>
@{
    User tmp = Session["User"] as User;
    if (tmp != null)
    {
        if ((tmp.UserType == "admin") || (tmp.UserType == "editor"))
        {
<p>
    @Html.ActionLink("添加课程", "Add", "ContestGroups", new { @class="btn btn-default" })
    @Html.ActionLink("删除课程", "Delete", "ContestGroups", new { @class="btn btn-default" })
</p>
        }
    }
}
<table class="table">
    <tr>
        <th>课程ID</th>
        <th>课程名称</th>
    </tr>
    @foreach (ContestGroup i in Model)
    {
        <tr>
            <td>@i.ContestGroupID</td>
            <td>@Html.ActionLink(@i.ContestGroupName, "Index", "Contests", new { id = @i.ContestGroupID }, null)</td>
        </tr>
    }
</table>